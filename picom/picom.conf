backend = "glx";
vsync = true;
glx-use-copysubbuffer-mesa = true;

glx-copy-from-front = true;
glx-swap-method = 2;
xrender-sync = true;
xrender-sync-fence = true;

# opacity-rule = [
  # "95:class_g = 'Alacritty'",  # 90% opacity for terminal windows
  # "100:class_g = 'org.mozilla.firefox'",   # 80% opacity for browser windows
  # "100:focused",                          # 95% opacity for focused windows
  # "85:!focused"                          # 85% opacity for unfocused windows
# ];